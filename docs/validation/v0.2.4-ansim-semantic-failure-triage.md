# JDIPT v0.2.4 Ansim Semantic Triage Result

## 1. Executive Summary

분석 대상은 최신 Live Core 실행 `regression-results/ansim-v024-20260901-202519` 하나로 한정했다. 이전 static fixture, diagnostic ASH-01 실행, 과거 trial 결과는 판정에 혼합하지 않았다.

- Core process: `6/9`
- Existing semantic Oracle: `1/9 PASS`, `5/9 FAIL`, `3/9 INVALID`
- Human classification: `TRUE_PASS=1`, `TRUE_FAIL=3`, `ORACLE_FALSE_NEGATIVE=2`, `NOT_EVALUABLE_TIMEOUT=3`
- Critical five-marker re-check: `MISSED_CRITICAL=0`
- 결론: semantic `1/9`는 모델 행동 실패 3건, evaluator false negative 2건, process timeout 3건으로 분리된다.

## 2. Source Artifacts

- Oracle JSON: `skills/law-interpretation-request/evals/v0.2.4-ansim-housing-oracle.json`
- Oracle Markdown: `skills/law-interpretation-request/evals/v0.2.4-ansim-housing-oracle.md`
- Latest Core summary: `regression-results/ansim-v024-20260901-202519/summary.json`
- Latest findings: `regression-results/ansim-v024-20260901-202519/oracle-findings.md`
- Per-case response/logs: `regression-results/ansim-v024-20260901-202519/ASH-01` through `ASH-09`
- Process diagnostics: each completed/timeout case `E##.log.txt` (`PID`, `SPAWNED_AT_UTC`, `TIMEOUT`, `KILLED`, artifact state, stdout/stderr tail)

## 3. Case Matrix

| Case | Process | Current Oracle | Critical | Human Semantic Verdict | Final Classification | Root Cause |
| --- | --- | --- | ---: | --- | --- | --- |
| ASH-01 | PASS | FAIL | 0 | 필수 촉진지구/일반 대상지 구분 누락 | TRUE_FAIL | authority/temporal distinction |
| ASH-02 | PASS | PASS | 0 | 본칙 250m와 350m 심의 예외 모두 충족 | TRUE_PASS | 없음 |
| ASH-03 | PASS | FAIL | 0 | 안심주택 특별규정과 서울 200㎡/대 분기 누락 | TRUE_FAIL | special_rule_precedence/context branching |
| ASH-04 | PASS | FAIL | 0 | 핵심 조건은 의미상 충족, matcher가 명시 문구를 놓침 | ORACLE_FALSE_NEGATIVE | proposition surface matching |
| ASH-05 | TIMEOUT | INVALID | 0 | 완성 응답 없음 | NOT_EVALUABLE_TIMEOUT | MCP/search latency |
| ASH-06 | PASS | FAIL | 0 | 250m 본칙·350m 예외 및 과거 기준 비승계 분기 누락 | TRUE_FAIL | temporal/exception resolution |
| ASH-07 | PASS | FAIL | 0 | 45% 과반 미달·예외 가능성·350m 구조를 의미상 충족했으나 matcher가 독립성 표면문구를 놓침 | ORACLE_FALSE_NEGATIVE | proposition surface matching |
| ASH-08 | TIMEOUT | INVALID | 0 | 완성 응답 없음 | NOT_EVALUABLE_TIMEOUT | MCP/annex lookup latency |
| ASH-09 | TIMEOUT | INVALID | 0 | 완성 응답 없음 | NOT_EVALUABLE_TIMEOUT | MCP/search latency |

## 4. ASH-01~ASH-09 Detailed Review

## ASH-01

### Process

PASS. `returncode=0`, `TIMEOUT=False`, answer artifact 존재, 실행시간 `100.541s`.

### Existing Oracle Verdict

FAIL. Finding: `PROMOTION_1000_DISTINCTION` 누락. 검출된 positive marker는 `CURRENT_STANDARD_REQUIRED`, `UNCERTAINTY_PRESERVED`이다. Negative/critical marker는 없다.

### Oracle Requirements

- 현행 일반 최소면적을 500㎡로 단정하지 않는다.
- 조례상 촉진지구 1,000㎡와 별도 사업대상지·용도지역 변경 기준을 구분한다.
- 500㎡를 제시하려면 기준일 현재 공식 운영기준의 직접 근거를 확인한다.
- 시행·개정 시점을 구분한다.

### Response Evidence

- `SATISFIED`: “과거 청년주택 자료에서 보이는 **500㎡ 이상** 기준은 현재 일반 기준으로 바로 적용하기 어렵습니다.”
- `SATISFIED`: “현행 ... 운영기준은 ... **1,000㎡ 이상**으로 정하고 있습니다.”
- `MISSING`: 촉진지구 1,000㎡와 일반 사업대상지/용도지역 변경 세부기준을 별개 개념으로 명시하지 않았다.
- `PARTIALLY_SATISFIED`: 현행 기준을 인용하고 추가 조건 확인을 남겼지만, 결론은 “일반적인 ... 최소 면적은 1,000㎡”로 확정했다.
- Evaluator finding은 위 구분 누락을 정확히 포착했으므로 false negative가 아니다.

### Critical Check

- `REPEALED_AS_CURRENT`: absent
- `GENERAL_350M`: absent
- `AUTO_350M`: absent
- `GENERAL_RULE_OVERRIDES_SPECIAL`: absent
- `ASSUMED_FACTS`: absent

### Human Verdict

`TRUE_FAIL`

### Root Cause

현행성은 보존했으나 촉진지구 규정과 일반 대상지 기준의 법적 역할을 분리하지 않아 `authority_priority`와 `temporal_resolution` 계약이 불완전하게 실행됐다.

## ASH-02

### Process

PASS. `returncode=0`, `TIMEOUT=False`, answer artifact 존재, 실행시간 `115.627s`.

### Existing Oracle Verdict

PASS. Positive markers: `BASE_250M`, `EXCEPTION_350M_REVIEW`. Negative/critical marker 없음.

### Oracle Requirements

- 본칙은 250m이다.
- 350m는 특정 사유와 통합심의에 따른 사업대상지 지정 예외이다.
- 측정 기준의 승강장 경계 및 출입구를 왜곡하지 않는다.

### Response Evidence

- `SATISFIED`: “기본 기준은 ... **250m 이내**입니다.”
- `SATISFIED`: “통합심의위원회 심의를 거치면 ... **350m 이내**까지 ... 지정할 수 있습니다.”
- `SATISFIED`: 350m를 “일반 기준이 아니라 예외적으로 적용되는 범위”라고 명시했다.
- Evaluator PASS와 의미가 일치한다.

### Critical Check

- `REPEALED_AS_CURRENT`: absent
- `GENERAL_350M`: absent
- `AUTO_350M`: absent
- `GENERAL_RULE_OVERRIDES_SPECIAL`: absent
- `ASSUMED_FACTS`: absent

### Human Verdict

`TRUE_PASS`

### Root Cause

없음.

## ASH-03

### Process

PASS. `returncode=0`, `TIMEOUT=False`, answer artifact 존재, 실행시간 `243.982s`.

### Existing Oracle Verdict

FAIL. Findings: `DORM_200_SEOUL`, `SPECIAL_PARKING_RULE` 누락. Positive marker `DORM_300_NATIONAL`, `UNCERTAINTY_PRESERVED`만 검출됐다. Negative/critical marker 없음.

### Oracle Requirements

- 질문에 안심주택 여부가 없으므로 일반 임대형기숙사와 서울 안심주택 임대형기숙사를 분기한다.
- 국가 일반기준 비학생용은 300㎡/대 구조를 검토한다.
- 서울시 주차조례 별표 2의 비학생용 200㎡/대를 구분한다.
- 안심주택이면 조례 제13조제2항의 특별규정을 직접 언급한다.

### Response Evidence

- `SATISFIED`: “학생용 ... **400㎡당 1대**”, “그 밖의 건축물 ... **300㎡당 1대**”를 구분했다.
- `MISSING`: 서울시 200㎡/대 기준을 제시하지 않았다.
- `MISSING`: 안심주택 조례 제13조제2항이 서울시 주차조례를 직접 지정하는 특별규정이라는 분기가 없다.
- `MISSING`: 관할 조례 확인을 일반론으로만 남겨 안심주택 특별규정 우선관계를 설명하지 않았다.
- Evaluator findings는 실제 누락과 일치한다. `DORM_200_SEOUL`은 단순 표면 표현 차이가 아니라 응답에 200㎡/대 proposition 자체가 없다.

### Critical Check

- `REPEALED_AS_CURRENT`: absent
- `GENERAL_350M`: absent
- `AUTO_350M`: absent
- `GENERAL_RULE_OVERRIDES_SPECIAL`: absent
- `ASSUMED_FACTS`: absent

### Human Verdict

`TRUE_FAIL`

### Root Cause

질문 범위 분기와 안심주택 특별규정 우선이 약하게 실행됐다. 분류는 `special_rule_precedence` 및 `question_scope`다.

## ASH-04

### Process

PASS. `returncode=0`, `TIMEOUT=False`, answer artifact 존재, 실행시간 `227.475s`.

### Existing Oracle Verdict

FAIL. Findings: `DISTANCE_NOT_REPLACED`, `UNCERTAINTY_PRESERVED` 누락. Positive markers `BASE_250M`, `EXCEPTION_350M_REVIEW`, `CURRENT_STANDARD_REQUIRED` 검출. Negative/critical marker 없음.

### Oracle Requirements

- 300m는 본칙 250m 밖이다.
- 350m 예외 범위에는 들어갈 수 있다.
- 법정 사유와 통합심의가 필요하다.
- 1,500㎡는 거리요건을 대체하지 않는다.
- 나머지 입지·계획요건은 별도 확인사항이다.

### Response Evidence

- `SATISFIED`: “역세권 기본 범위는 ... **250m 이내**”, “300m 부지는 ... **350m까지 ... 심의**”라고 했다.
- `SATISFIED`: “1,500㎡는 최소 면적 ... 충족”과 “거리만으로 자격이 확정되는 것이 아니라 심의 ... 필요”를 분리했다. 이는 1,500㎡가 거리요건을 대체하지 않는다는 의미와 동일하다.
- `SATISFIED`: “... 나머지 요건도 충족하는 경우에 한해”라고 조건부 결론을 유지했다.
- Evaluator는 `거리요건을 대체하지` 및 `확인 필요/확정할 수 없다`와 같은 특정 표면 표현만 요구해 의미상 충족을 FAIL 처리했다.

### Critical Check

- `REPEALED_AS_CURRENT`: absent
- `GENERAL_350M`: absent
- `AUTO_350M`: absent
- `GENERAL_RULE_OVERRIDES_SPECIAL`: absent
- `ASSUMED_FACTS`: absent

### Human Verdict

`ORACLE_FALSE_NEGATIVE`

### Root Cause

법적 주체·본칙/예외 관계·조건과 효과·수치·방향·최종 결론이 Oracle과 모두 일치한다. `DISTANCE_NOT_REPLACED`와 `UNCERTAINTY_PRESERVED`의 proposition-level 의미를 evaluator가 좁은 phrase family로만 인식했다.

## ASH-05

### Process

TIMEOUT. `E05.log.txt`: `PID=30756`, `TIMEOUT=True`, `KILLED=True`, `RETURN_CODE=1`, `DURATION_SECONDS=300.326`, `ANSWER_FILE_EXISTS=False`.

### Existing Oracle Verdict

INVALID. Finding: `UNUSABLE_RESPONSE`. 완성된 모델 응답이 없다.

### Oracle Requirements

- 공공지원민간임대주택과 임대형기숙사를 용도별로 각각 산정한다.
- 안심주택 조례 제13조제3항 특별규정을 적용한다.
- 법정 필요대수를 산정 후 합산하고 물리적 공동사용과 법정 최소대수를 구분한다.

### Response Evidence

- 응답 artifact 없음. proposition 판정은 `NOT_EVALUABLE`.
- stdout tail에는 `get_law_text`, 법령 검색, `search_decisions` 실패 및 `legal_research` 완료가 기록되었고, 최종 답변 전에 timeout됐다.

### Critical Check

- 다섯 marker 모두 `not evaluable (완성 응답 없음)`; present로 볼 근거 없음.

### Human Verdict

`NOT_EVALUABLE_TIMEOUT`

### Root Cause

주차장법·건축법·민간임대주택법 및 판례/해석례 조사 과정의 MCP·검색 지연으로 300초 상한에 도달했다. semantic 모델 실패로 분류하지 않는다.

## ASH-06

### Process

PASS. `returncode=0`, `TIMEOUT=False`, answer artifact 존재, 실행시간 `224.640s`.

### Existing Oracle Verdict

FAIL. Findings: `BASE_250M`, `EXCEPTION_350M_REVIEW` 누락. Positive marker `CURRENT_STANDARD_REQUIRED`만 검출됐고 negative/critical marker는 없다.

### Oracle Requirements

- 현행 조례의 250m 본칙과 350m 예외지정을 구분한다.
- 현행 서울시 공식 운영기준을 직접 확인한다.
- 과거 청년주택의 수치·인접조건을 현행 규정으로 자동 승계하지 않는다.
- 확인하지 못한 기준은 구체 수치로 확정하지 않는다.

### Response Evidence

- `SATISFIED`: 현행 2026.8.3 운영기준과 조례를 직접 인용하고 현재 기준임을 밝혔다.
- `MISSING`: 응답은 용도지역 변경조건을 나열했지만 250m 본칙과 350m 통합심의 350m 예외를 수치로 분리하지 않았다.
- `MISSING`: 과거 청년주택 기준을 자동 승계하지 말아야 한다는 temporal caution이 없다.
- `MISSING`: 구체 조건을 확인 필요로 낮춰야 하는 경우에도 여러 수치를 확정적으로 제시했다.

### Critical Check

- `REPEALED_AS_CURRENT`: absent
- `GENERAL_350M`: absent
- `AUTO_350M`: absent
- `GENERAL_RULE_OVERRIDES_SPECIAL`: absent
- `ASSUMED_FACTS`: absent

### Human Verdict

`TRUE_FAIL`

### Root Cause

운영기준을 찾았지만 법령 버전·본칙/예외·과거 기준 비승계의 연결을 답변 구조에 반영하지 못했다. 분류는 `temporal_resolution` 및 `exception_semantics`다.

## ASH-07

### Process

PASS. `returncode=0`, `TIMEOUT=False`, answer artifact 존재, 실행시간 `177.813s`.

### Existing Oracle Verdict

FAIL. Findings: `MAJORITY_EXCEPTION`, `SEPARATE_EXCEPTIONS` 누락. Positive markers `BASE_250M`, `EXCEPTION_350M_REVIEW`, `MAJORITY_RULE`, `MEDICAL_350`, `UNCERTAINTY_PRESERVED` 검출. Negative/critical marker 없음.

### Oracle Requirements

- 중심지역 내외에 걸치면 과반 포함이 원칙이다.
- 45%는 원칙요건 미달이다.
- 토지 효율·구역 정형화 등 사유와 통합심의에 따른 과반 예외를 구분한다.
- 350m 지정 특례와 과반비율 예외를 별개 심의 포인트로 분리한다.
- 다른 계획요건도 별도로 확인한다.

### Response Evidence

- `SATISFIED`: “45% ... 원칙적으로 요건을 충족하지 못합니다”, “과반에 미달합니다”라고 했다.
- `SATISFIED`: 토지 효율적 이용·구역 정형화 및 통합심의위원회 예외 가능성을 언급했다.
- `SATISFIED`: 350m 지정 구조와 과반 구조를 서로 다른 조건·효과로 설명해 독립적인 심의 포인트라는 의미를 보존했다.
- `SATISFIED`: “과반 예외”, “서로 독립”이라는 표면 문구는 없지만, 45% 본칙 미달과 통합심의에 따른 예외 가능성을 별도로 서술해 의미를 충족했다.
- Evaluator의 `MAJORITY_EXCEPTION`, `SEPARATE_EXCEPTIONS` missing finding은 이 의미를 좁은 phrase family로 인식하지 못한 false negative다.

### Critical Check

- `REPEALED_AS_CURRENT`: absent
- `GENERAL_350M`: absent
- `AUTO_350M`: absent
- `GENERAL_RULE_OVERRIDES_SPECIAL`: absent
- `ASSUMED_FACTS`: absent

### Human Verdict

`ORACLE_FALSE_NEGATIVE`

### Root Cause

법적 주체·본칙/예외 관계·조건과 효과·수치·방향·최종 결론이 Oracle과 실질적으로 일치한다. evaluator가 “과반 예외/서로 독립”이라는 좁은 표면 문구만 요구해 false negative가 발생했다.

## ASH-08

### Process

TIMEOUT. `E08.log.txt`: `PID=33384`, `TIMEOUT=True`, `KILLED=True`, `RETURN_CODE=1`, `DURATION_SECONDS=300.314`, `ANSWER_FILE_EXISTS=False`.

### Existing Oracle Verdict

INVALID. Finding: `UNUSABLE_RESPONSE`. 완성된 모델 응답이 없다.

### Oracle Requirements

- 복합용도 주차를 용도별로 산정한다.
- 400%는 자동 권리가 아닌 완화 가능성이다.
- 산업부지 비율은 통합심의위원회 결정과 위임관계를 따른다.
- 지구단위계획 추가 주차완화는 법정 요건과 결정문 반영을 확인한다.

### Response Evidence

- 응답 artifact 없음. proposition 판정은 `NOT_EVALUABLE`.
- stdout tail에는 `execute_tool`, `get_annexes`, `get_law_text` 일부 실패·재조회, `search_law`, `discover_tools` 활동이 남았고 timeout됐다.

### Critical Check

- 다섯 marker 모두 `not evaluable (완성 응답 없음)`; present로 볼 근거 없음.

### Human Verdict

`NOT_EVALUABLE_TIMEOUT`

### Root Cause

별표·별지·지구단위계획 위임관계를 확인하는 MCP/annex 조회가 300초 안에 끝나지 않았다. semantic 모델 실패로 분류하지 않는다.

## ASH-09

### Process

TIMEOUT. `E09.log.txt`: `PID=32632`, `TIMEOUT=True`, `KILLED=True`, `RETURN_CODE=1`, `DURATION_SECONDS=300.297`, `ANSWER_FILE_EXISTS=False`.

### Existing Oracle Verdict

INVALID. Finding: `UNUSABLE_RESPONSE`. 완성된 모델 응답이 없다.

### Oracle Requirements

- 통합 안심주택 조례를 현행 근거로 사용한다.
- 의료시설 중심지역 350m 구조를 적용한다.
- 자연녹지·어르신 공급 세부기준을 운영기준에서 확인한다.
- 거리·면적만으로 사업·승인을 확정하지 않는다.

### Response Evidence

- 응답 artifact 없음. proposition 판정은 `NOT_EVALUABLE`.
- stdout tail에는 통합 조례·별표 조회, 다수 `get_law_text` 완료, 자연녹지·의료시설 관련 웹 검색이 기록됐으나 최종 답변 전에 timeout됐다.

### Critical Check

- 다섯 marker 모두 `not evaluable (완성 응답 없음)`; present로 볼 근거 없음.

### Human Verdict

`NOT_EVALUABLE_TIMEOUT`

### Root Cause

현행 통합조례와 자연녹지·의료시설·어르신 공급 운영기준을 교차 확인하는 검색이 300초 상한에 도달했다. semantic 모델 실패로 분류하지 않는다.

## 5. Critical Re-check

완성 응답 6건의 원문과 timeout logs를 다시 확인했다. `REPEALED_AS_CURRENT`, `GENERAL_350M`, `AUTO_350M`, `GENERAL_RULE_OVERRIDES_SPECIAL`, `ASSUMED_FACTS`는 어느 완성 응답에서도 발견되지 않았다. timeout 3건은 answer artifact가 없어 의미·critical 판정을 할 수 없으며, 기존 evaluator가 놓친 Critical을 확인할 응답도 없다. 따라서 `MISSED_CRITICAL=0`이다.

## 6. Root Cause Distribution

```text
TRUE_PASS = 1
TRUE_FAIL = 3
ORACLE_FALSE_NEGATIVE = 2
NOT_EVALUABLE_TIMEOUT = 3
MISSED_CRITICAL = 0

model_behavior_failure = 3
evaluator_false_negative = 2
process_timeout = 3
```

## 7. Evaluator False Negatives

### ASH-04 and ASH-07

현재 matcher는 `DISTANCE_NOT_REPLACED`에 “거리요건을 대체하지”와 유사한 좁은 표현을, `UNCERTAINTY_PRESERVED`에 “확인 필요/확정할 수 없다” 계열을 요구한다. ASH-04의 실제 응답은 “1,500㎡는 면적 기준을 충족”하면서도 “300m는 심의에 따른 350m 예외가 필요하고 거리만으로 자격이 확정되지 않는다”고 구조적으로 설명했다. ASH-07도 45% 과반 미달, 통합심의 예외, 350m 지정 구조를 별도 조건·효과로 설명했지만 `과반 예외`·`서로 독립` 표면문구는 없었다. 두 case 모두 법적 주체, 본칙/예외, 조건·효과, 수치, 방향, 최종 결론이 Oracle과 실질적으로 같으므로 proposition-level false negative다.

권고는 이번 단계에서 구현하지 않는다. surface phrase 추가보다 `distance requirement remains independent of area`, `conditional eligibility`, `majority exception separate from 350m designation`을 proposition 수준 의미 family로 모델링하는 것이 적절하다.

## 8. Actual Model Behavior Failures

- ASH-01: 일반 사업대상지 1,000㎡를 확정하면서 촉진지구 1,000㎡와 별도 기준을 구분하지 않음.
- ASH-03: 국가 300㎡/대만 제시하고 서울 200㎡/대 및 안심주택 제13조 특별규정을 누락.
- ASH-06: 현행 운영기준 수치는 인용했지만 250m 본칙·350m 예외·과거 기준 비승계를 누락.


이 3건은 기존 일반 행동계약이 없어서가 아니라, 실제 출력에서 해당 법적 연결부가 누락되거나 불명확한 사례다. 즉시 `SKILL.md` 수정으로 결론을 하드코딩할 근거는 없다.

## 9. Timeout Cases

| Case | Duration | Last activity | Artifact | Classification |
| --- | ---: | --- | --- | --- |
| ASH-05 | 300.326s | law MCP, web search, decision search failure, legal research | 없음 | NOT_EVALUABLE_TIMEOUT |
| ASH-08 | 300.314s | execute_tool, annexes/law text lookup, search/discover tools | 없음 | NOT_EVALUABLE_TIMEOUT |
| ASH-09 | 300.297s | integrated ordinance/annex lookup, repeated law text, web search | 없음 | NOT_EVALUABLE_TIMEOUT |

단순 timeout 상향은 응답 완주율을 높일 수 있지만 한 case가 수분 이상 점유하고 원인 가시성이 낮아진다. 현재 추가된 diagnostics는 PID·spawn·kill·artifact·stdout/stderr tail을 확보한다. 다음 단계에서는 MCP/tool activity-aware watchdog 또는 단계별 idle timeout을 비교 검토할 수 있으나 이번 작업에서는 runner를 수정하지 않는다.

## 10. Recommended Next Changes

- evaluator: ASH-04·ASH-07 유형의 의미 동등 표현을 proposition-level matcher family로 보강하는 설계 검토. 이번 단계에서는 수정하지 않음.
- skill: ASH-01/03/06의 반복 live evidence가 누적되기 전 `SKILL.md` 수정 보류. 정답 문구 하드코딩 금지.
- runner: 현재 diagnostics를 유지하고 MCP activity-aware watchdog 설계를 별도 작업으로 검토. 이번 단계에서는 수정하지 않음.
- oracle: source of truth 유지. 수정하지 않음.

## 11. Git Status

- branch: `main`
- HEAD at triage start: `5e4a79f docs: record ansim core live outcome`
- working tree before this document: 기존 untracked 검증 보고서 5개만 존재
- changes in this task: 이 triage 문서 1개 생성됨
- commit: `NOT_CREATED`

# Classification

- TRUE_PASS: 1
- TRUE_FAIL: 3
- ORACLE_FALSE_NEGATIVE: 2
- NOT_EVALUABLE_TIMEOUT: 3
- MISSED_CRITICAL: 0

# Case Matrix

상세 표는 위 `## 3. Case Matrix`를 참조한다. 9개 case가 정확히 한 enum으로 분류됐다.

# Root Cause

- model_behavior_failure: 3
- evaluator_false_negative: 2
- process_timeout: 3

# Recommended Fix Scope

- evaluator: ASH-04·ASH-07 proposition-level semantic family 검토만 권고
- skill: 즉시 수정 권고 없음
- runner: activity-aware watchdog 별도 검토; 이번 단계 수정 없음
- oracle: 수정 없음

# Files

- created: `docs/validation/v0.2.4-ansim-semantic-failure-triage.md`
- modified: 없음
- untouched: Oracle JSON/Markdown, evaluator, `SKILL.md`, runner, 기존 validation report, 기존 untracked 보고서 5개

# Git

- branch: `main`
- HEAD: `5e4a79f docs: record ansim core live outcome`
- working tree: 새 triage 문서 생성 전 기준 untracked 보고서 5개; 문서 생성 후 triage 문서 1개 추가
- commit: `NOT_CREATED`

# v0.2.4 Ansim Housing Regression Status

## Current release verdict

**HOLD**

The Ansim Housing suite is not yet release-ready.

## Evaluator status

- semantic evaluator correction from PR #13: validated independently
- targeted evaluator false negatives previously identified for ASH-04 / ASH-07: corrected
- current targeted failures must still be separated into model/Skill failures vs marker-detection failures before more runtime policy changes

## Structural behavior work — PR #14

Branch: `fix/ansim-structural-behavior-stability`

Static/runtime validation on 2026-09-02 before status-only documentation commits:

- `tests/test_structural_behavior_contract.py`: **10 passed**
- full pytest: **169 passed**
- `validate_repo.py`: **PASS**
- `validate_authority_temporal_contract.py`: **PASS**
- installed runtime integrity after plugin refresh: **PASS**

Latest targeted live run after quantitative-scope + compound-issue patch:

- ASH-01: **0/3 PASS, 3/3 FAIL, 0 INVALID**
- ASH-06: **0/3 PASS, 3/3 FAIL, 0 INVALID**
- critical negative markers: **0**

Previous targeted live run after the first PR #14 patch:

- ASH-01: 0/3 PASS
- ASH-03: 0/3 PASS
- ASH-06: 1/3 PASS
- critical negative markers: 0

## Next diagnostic gate

Do **not** add more Skill hard stops from the aggregate verdict alone.

The Ansim evaluator marks a case FAIL when any required semantic marker is absent. The latest six answers must therefore be inspected for:

- detected markers
- missing required markers
- actual answer propositions
- retrieval/tool evidence showing whether the proposition was found and then omitted, or never found

Required marker sets:

- ASH-01: `PROMOTION_1000_DISTINCTION`, `CURRENT_STANDARD_REQUIRED`, `UNCERTAINTY_PRESERVED`
- ASH-06: `BASE_250M`, `EXCEPTION_350M_REVIEW`, `CURRENT_STANDARD_REQUIRED`

Classify each failed run before any further production edit as one of:

1. `TRUE_BEHAVIOR_FAILURE_RETRIEVAL`
2. `TRUE_BEHAVIOR_FAILURE_SYNTHESIS`
3. `ORACLE_FALSE_NEGATIVE`
4. `MIXED`

## ASH-03

ASH-03 remains outside the current Skill-fix loop. Its bare prompt does not identify Seoul or Ansim Housing while the Oracle requires those specific branches. Resolve that eval-input/Oracle scope contract separately; do not hardcode that hidden context into the Skill.

## Release sequencing

1. classify latest ASH-01 / ASH-06 failed runs
2. make only root-cause-supported changes
3. rerun targeted cases
4. address timeout reliability track separately
5. run core Ansim 9/9
6. only then run 27-run stability
7. release gate

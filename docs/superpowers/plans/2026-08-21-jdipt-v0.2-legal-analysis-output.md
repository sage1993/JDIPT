# JDIPT v0.2 Legal Analysis and Output Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** JDIPT의 기본 법률검토 동작을 `법적 쟁점 매핑 → 법령해석 → 논리검증 → 4단 Answer-first 출력` 구조로 전환하고, 단일 쟁점의 논증을 과도한 소제목 없이 연속적으로 작성하도록 한다.

**Architecture:** 기존 Skill-first 구조와 Korean Law MCP 경계는 유지한다. `references/legal-issue-mapping.md`를 새 내부 Gate 계약으로 추가하고 기존 `interpretation-principles.md`와 `logic-validation.md`의 책임을 유지한다. 사용자 출력은 기본 1~6 구조를 폐기하고 `질의요지 → 검토결론 → 검토이유 → 관련 법령 및 자료` 4단 구조로 마이그레이션하되, 법제처 제출용 1~3 구조는 그대로 유지한다.

**Tech Stack:** Markdown Skill/Reference/Eval 문서, Python 3.11+ 정적 validator, Node.js >=20.19.0, Codex Plugin manifest, `korean-law-mcp` 4.12.1.

**Spec:** `docs/superpowers/specs/2026-08-21-jdipt-v0.2-legal-analysis-output-design.md`

## Global Constraints

- 기본 법률검토형 H1은 정확히 `# 1. 질의요지`, `# 2. 검토결론`, `# 3. 검토이유`, `# 4. 관련 법령 및 자료` 순서로 사용한다.
- 상세 검토이유보다 검토결론을 먼저 제시한다.
- 단일 쟁점에서는 `법적 정의`, `적용 규정`, `사안 적용`, `문제 발생 지점`, `해석`을 각각 별도 소제목으로 기계적으로 분리하지 않는다.
- `# 3. 검토이유`의 하위 소제목은 서로 독립적으로 판단 가능한 복수 쟁점이 있을 때만 사용한다.
- Legal Issue Mapping은 내부 절차이며 내부 라벨·중간표를 기본 사용자 출력에 노출하지 않는다.
- 법제처 제출용 1~3 구조는 변경하지 않는다.
- `references/logic-validation.md`의 점수체계, BLOCK, 추상 입력 보존, 비노출 계약은 변경하지 않는다.
- `korean-law-mcp` 버전은 `4.12.1`로 유지하고 MCP 도구 인터페이스를 변경하지 않는다.
- JDIPT Plugin 버전은 `0.2.0`으로 올린다.
- 확인되지 않은 사실·판례·해석례·URL을 생성하지 않는다.
- 모든 사용자용 최종 출력은 Markdown을 유지한다.

---

### Task 1: Legal Issue Mapping 계약 추가

**Files:**
- Create: `skills/law-interpretation-request/references/legal-issue-mapping.md`
- Modify: `scripts/validate_repo.py`

**Interfaces:**
- Consumes: 기존 `interpretation-principles.md`, `logic-validation.md`의 책임 경계.
- Produces: 이후 `SKILL.md`가 호출할 Legal Issue Mapping contract.

- [ ] **Step 1: validator에 새 reference 요구조건을 먼저 추가한다**

`REQUIRED_REFERENCES`에 `"legal-issue-mapping.md"`를 추가하고 다음 marker set을 추가한다.

```python
REQUIRED_ISSUE_MAPPING_MARKERS = {
    "법적 쟁점 매핑 Gate",
    "주체",
    "행위",
    "법적 상태 또는 분류",
    "적용 규범 지도",
    "동일 사항의 중복 규율",
    "규율 공백",
    "충족",
    "불충족",
    "확인 필요",
    "문제 발생 지점",
}
```

`main()`에서 `legal-issue-mapping.md`를 읽고 `require_markers()`로 검사한다.

- [ ] **Step 2: validator가 실패하는지 확인한다**

```bash
python scripts/validate_repo.py
```

Expected: 새 reference가 아직 없으므로 FAIL.

- [ ] **Step 3: `legal-issue-mapping.md`를 작성한다**

```markdown
# 법적 쟁점 매핑 Gate

## 1. 질문 요소 특정
- 주체
- 행위
- 대상
- 법적 상태 또는 분류
- 규모·수치·시점
- 확인하려는 법적 결과

## 2. 법적 정의·분류 확인
법정 정의 → 상·하위 개념 → 포함·제외 → 다른 법령 분류 연결 순으로 확인한다.

## 3. 적용 규범 지도
정의 / 본칙 / 적용요건 / 예외 / 특례 / 위임 / 적용 제외 / 준용 / 다른 법률 규정으로 역할을 분류한다.

## 4. 규정 관계 진단
동일 사항의 중복 규율, 일반/특별, 누적 적용, 원칙/예외, 특례/특례, 준용, 적용 제외, 규율 공백, 서로 다른 규율대상을 구분한다.

## 5. 사실관계와 요건 연결
각 핵심 요건은 충족 / 불충족 / 확인 필요 중 하나로 관리한다.

## 6. 문제 발생 지점
결과를 반복하지 말고 결론을 가르는 법적 연결부를 한 문장으로 특정한다.
```

마지막에 내부 mapping 라벨과 상태표는 별도 요청이 없는 한 사용자 출력에 그대로 노출하지 않는다고 명시한다.

- [ ] **Step 4: validator 통과를 확인한다**

```bash
python scripts/validate_repo.py
```

Expected: PASS.

- [ ] **Step 5: 커밋한다**

```bash
git add -- skills/law-interpretation-request/references/legal-issue-mapping.md scripts/validate_repo.py
git commit -m "feat: add legal issue mapping contract"
```

---

### Task 2: Legal Issue Mapping을 Skill 실행 순서에 연결

**Files:**
- Modify: `skills/law-interpretation-request/SKILL.md` (`## 작업 순서`)
- Modify: `skills/law-interpretation-request/references/interpretation-principles.md`
- Modify: `scripts/validate_repo.py`

**Interfaces:**
- Consumes: `references/legal-issue-mapping.md`.
- Produces: `Legal Issue Mapping → Legal Interpretation → Logic Validation → Answer Rendering` 실행 순서.

- [ ] **Step 1: validator가 Skill의 새 Gate 참조를 요구하도록 만든다**

Skill marker set에 다음 문자열을 추가한다.

```python
"references/legal-issue-mapping.md",
"법적 쟁점 매핑 Gate",
"문제 발생 지점",
"규율 공백",
```

- [ ] **Step 2: 현재 Skill에서 실패하는지 확인한다**

```bash
python scripts/validate_repo.py
```

Expected: 새 Skill marker missing으로 FAIL.

- [ ] **Step 3: `SKILL.md` 작업 순서를 재배치한다**

작업 순서를 다음 책임 순서로 바꾼다.

```markdown
1. 질문·사실관계·요구사항과 사용 맥락 확인
2. eligibility-checklist 적합성 점검
3. 필수 정보가 부족한 경우에만 필요한 질문
4. 공식 법령·판례·해석례 조사와 현행성 확인
5. legal-issue-mapping에 따른 법적 대상·정의·규정 구조·규정 관계·사실대입·문제 발생 지점 특정
6. interpretation-principles와 case-patterns에 따른 필요한 법령해석
7. 법적 논증 초안 작성
8. logic-validation에 따른 검증, BLOCK 수정·재검증
9. 출처·현행성 최종 확인
10. 출력 모드에 따른 Markdown 최종 문안 작성
```

추상 A/B/P/Q 시나리오는 기존대로 실제 법령에 임의 대응하지 않는 규칙을 유지한다.

- [ ] **Step 4: `interpretation-principles.md`의 책임 경계를 명시한다**

다음 문장을 해석 순서 앞에 추가한다.

```markdown
이 문서는 `legal-issue-mapping.md`에서 법적 대상, 적용 규정, 규정 관계와 문제 발생 지점을 특정한 뒤 실제 규범의 의미·범위를 해석하는 단계에 적용한다. 법적 대상과 규정 관계를 특정하지 않은 채 해석방법부터 적용하지 않는다.
```

기존 문언 → 정의/참조 → 체계 → 목적 → 연혁 → 판례·해석례 순서는 유지한다.

- [ ] **Step 5: validator를 실행한다**

```bash
python scripts/validate_repo.py
```

Expected: PASS.

- [ ] **Step 6: 커밋한다**

```bash
git add -- skills/law-interpretation-request/SKILL.md skills/law-interpretation-request/references/interpretation-principles.md scripts/validate_repo.py
git commit -m "feat: wire issue mapping into legal analysis flow"
```

---

### Task 3: 기본 출력 계약을 4단 Answer-first 구조로 마이그레이션

**Files:**
- Modify: `skills/law-interpretation-request/SKILL.md` (`## 출력 모드 선택`)
- Modify: `skills/law-interpretation-request/references/request-format.md`
- Modify: `AGENTS.md`
- Modify: `scripts/validate_repo.py`

**Interfaces:**
- Consumes: Task 2의 분석 흐름.
- Produces: 정확한 4개 H1, Answer-first, Narrative Coherence, 복수 쟁점 소제목 조건.

- [ ] **Step 1: validator를 새 출력 계약으로 먼저 바꾼다**

`REQUIRED_OUTPUT_SKILL_MARKERS`와 `REQUIRED_REQUEST_FORMAT_MARKERS`의 기본 출력 marker를 다음으로 교체한다.

```python
"# 1. 질의요지",
"# 2. 검토결론",
"# 3. 검토이유",
"# 4. 관련 법령 및 자료",
"검토결론을 상세 검토이유보다 먼저",
"서로 독립적으로 판단 가능한 복수의 법적 쟁점",
```

구 기본 구조가 다시 활성 계약으로 들어오지 못하게 `기본 1~6`, `# 1. 요청취지`, `# 5. 법률검토`, `# 6. 첨부자료`가 기본 모드 설명에 남아 있으면 FAIL하도록 검사한다. 법제처 제출용 1~3 구조는 legacy 검사에서 제외한다.

- [ ] **Step 2: 새 validator가 기존 문서에서 실패하는지 확인한다**

```bash
python scripts/validate_repo.py
```

Expected: 새 4단 marker missing 또는 구 1~6 active contract remaining으로 FAIL.

- [ ] **Step 3: `request-format.md` 기본 모드를 교체한다**

```markdown
# 1. 질의요지
질문의 핵심 법적 판단사항과 결론에 필요한 최소 사실만 정리한다.

# 2. 검토결론
상세 이유보다 먼저 1~3문장으로 결론을 제시한다. 확정할 수 없으면 조건부 결론과 확인 필요사항을 명시한다.

# 3. 검토이유
법적 정의, 적용 규정, 규정 관계, 사실대입, 문제 발생 지점과 해석을 하나의 연결된 논증으로 작성한다. 단일 쟁점의 분석 단계를 각각 별도 소제목으로 기계적으로 분리하지 않는다. 서로 독립적으로 판단 가능한 복수의 법적 쟁점이 있을 때만 `##` 소제목을 사용한다. 마지막에는 `# 2. 검토결론`과 일치하는 결론을 다시 확인한다.

# 4. 관련 법령 및 자료
최종 판단에 실제 사용한 법령·판례·해석례·행정규칙·첨부자료와 확인된 공식 링크를 정리한다.
```

법제처 제출용 1~3 형식은 수정하지 않는다.

- [ ] **Step 4: `SKILL.md` 기본 출력 모드를 같은 계약으로 동기화한다**

다음 원칙을 명시한다.

```markdown
- `# 2. 검토결론`에서 `# 3. 검토이유`보다 먼저 실제 결론을 제시한다.
- 내부 분석 단계의 이름을 사용자용 소제목으로 그대로 변환하지 않는다.
- 단일 쟁점에서는 정의·규정·사안 적용·문제지점·해석을 연속 논증으로 쓴다.
- 복수의 독립 쟁점이 있을 때만 `# 3. 검토이유` 내부에 `##` 소제목을 둔다.
```

- [ ] **Step 5: `AGENTS.md`의 저장소 수준 계약을 같은 4단 구조로 바꾼다**

```markdown
# 1. 질의요지
# 2. 검토결론
# 3. 검토이유
# 4. 관련 법령 및 자료
```

기존 법제처 제출용 1~3, Markdown, 공식자료 링크, 논리검증 규칙은 유지한다.

- [ ] **Step 6: validator를 실행한다**

```bash
python scripts/validate_repo.py
```

Expected: PASS.

- [ ] **Step 7: 커밋한다**

```bash
git add -- skills/law-interpretation-request/SKILL.md skills/law-interpretation-request/references/request-format.md AGENTS.md scripts/validate_repo.py
git commit -m "feat: adopt answer-first four-section output"
```

---

### Task 4: 법제처 샘플 기반 해석 패턴과 Narrative Coherence 추가

**Files:**
- Modify: `skills/law-interpretation-request/references/case-patterns.md`
- Modify: `scripts/validate_repo.py`

**Interfaces:**
- Consumes: 22-0351, 17-0047, 20-0604에서 추출한 논증 구조.
- Produces: 법적 분류형, 중복규율형, 규율공백형 내부 패턴.

- [ ] **Step 1: validator가 세 패턴 marker를 요구하도록 추가한다**

```python
REQUIRED_CASE_PATTERN_MARKERS = {
    "22-0351",
    "법적 분류형",
    "17-0047",
    "중복규율형",
    "20-0604",
    "규율공백형",
    "연속된 논증",
}
```

- [ ] **Step 2: validator 실패를 확인한다**

```bash
python scripts/validate_repo.py
```

Expected: 신규 case pattern marker missing으로 FAIL.

- [ ] **Step 3: 세 패턴을 `case-patterns.md`에 추가한다**

```markdown
| 22-0351 | 법적 분류형 | 대상의 법정 정의와 상·하위 분류를 확정한 뒤 본칙과 예외 중 어느 규율에 속하는지 판단 |
| 17-0047 | 중복규율형 | 두 규정이 동일 사항을 규율하는지 확인한 뒤 일반/특별 및 배타적 적용 여부 판단 |
| 20-0604 | 규율공백형 | 특별 규율체계에 해당 사항의 직접 규정이 존재하는지 확인하고 공백이면 일반법의 보충 적용 여부 판단 |
```

작성 원칙에 다음을 추가한다.

```markdown
하나의 쟁점을 해결하기 위한 정의 → 규정 → 사안 적용 → 해석 단계는 원칙적으로 별도 소제목으로 분절하지 않고 연속된 논증으로 쓴다. 독립 쟁점이 둘 이상인 경우에만 쟁점별 소제목을 둔다.
```

- [ ] **Step 4: validator를 실행한다**

```bash
python scripts/validate_repo.py
```

Expected: PASS.

- [ ] **Step 5: 커밋한다**

```bash
git add -- skills/law-interpretation-request/references/case-patterns.md scripts/validate_repo.py
git commit -m "feat: add legal interpretation case patterns"
```

---

### Task 5: Eval 계약을 4단 출력과 E27~E38로 확장

**Files:**
- Modify: `skills/law-interpretation-request/evals/expected-behavior.md`
- Modify: `skills/law-interpretation-request/evals/scenarios.md`
- Modify: `scripts/validate_repo.py`

**Interfaces:**
- Consumes: Tasks 1~4의 새 행동 계약.
- Produces: E1~E38 평가계약과 Golden Case 3종.

- [ ] **Step 1: validator에 신규 eval marker를 먼저 추가한다**

```python
REQUIRED_ISSUE_MAPPING_EVAL_MARKERS = {
    "E27. 법적 대상·정의·분류",
    "E28. 본칙·예외 선택",
    "E29. 동일 사항 중복규율",
    "E30. 규율공백과 충돌 구분",
    "E31. 사실관계와 법적 요건 연결",
    "E32. 문제 발생 지점 특정",
    "E33. Answer-first",
    "E34. 단일 쟁점 Narrative Coherence",
    "E35. 복수 독립 쟁점 소제목",
    "E36. 22-0351 법적 분류형 Golden Case",
    "E37. 17-0047 중복규율형 Golden Case",
    "E38. 20-0604 규율공백형 Golden Case",
}
```

`REQUIRED_OUTPUT_EVAL_MARKERS`의 `E21. 기본 1~6 출력`은 `E21. 기본 4단 출력`으로 바꾼다.

- [ ] **Step 2: 현재 eval 파일이 실패하는지 확인한다**

```bash
python scripts/validate_repo.py
```

Expected: E21 새 marker 및 E27~E38 marker missing으로 FAIL.

- [ ] **Step 3: `expected-behavior.md`의 기본 출력 계약을 마이그레이션한다**

다음을 통과조건으로 명시한다.

```markdown
- 기본 법률검토형은 정확한 H1 4단 구조를 사용한다.
- `# 2. 검토결론`에서 상세 이유보다 먼저 결론을 제시한다.
- `# 3. 검토이유`는 하나의 쟁점에서 정의·규정·사안 적용·해석을 과도하게 소제목으로 나누지 않는다.
- 서로 독립적으로 판단 가능한 복수 쟁점일 때만 검토이유 내부 소제목을 사용한다.
- 앞의 검토결론과 검토이유 말미의 결론은 모순되지 않는다.
- 내부 Issue Mapping 라벨·상태표를 기본 사용자 출력에 노출하지 않는다.
```

Plugin 적용 조건도 정확한 4단 H1로 갱신한다.

- [ ] **Step 4: 기존 출력 의존 시나리오를 마이그레이션한다**

- E19: 기본 1~6 → 기본 4단. 비노출 테스트라는 목적은 유지.
- E21: `기본 4단 출력`으로 변경.
- E23: Markdown + 4단 H1 검증.
- E25: `요청취지 유추`를 `질의요지 사실 왜곡 방지`로 변경. 제공되지 않은 사업목적·처분경위·기관입장을 생성하지 않는 목적은 유지.
- E26: Plugin 자동 적용 기대 구조를 4단 H1로 변경.

E1~E18과 E20의 실질 의미는 변경하지 않는다.

- [ ] **Step 5: E27~E35를 추가한다**

```markdown
## E27. 법적 대상·정의·분류
기대: 사용자 명칭을 그대로 법적 효과에 연결하지 않고 법정 정의·상하위 분류를 확인한다.

## E28. 본칙·예외 선택
기대: 대상이 예외요건에 실제 해당하는지 확인하고 해당하지 않으면 본칙을 적용한다.

## E29. 동일 사항 중복규율
기대: 두 규정이 동일 사항을 규율하는지 확인한 뒤 일반/특별 관계와 배타적 적용 여부를 검토한다.

## E30. 규율공백과 충돌 구분
기대: 특별 규율체계가 질문 대상을 직접 규정하지 않으면 충돌로 오인하지 않고 일반법 보충 적용을 검토한다.

## E31. 사실관계와 법적 요건 연결
기대: 확인된 사실만 요건에 연결하고 자료가 없는 요건은 확인 필요로 남긴다.

## E32. 문제 발생 지점 특정
기대: 결과 질문을 반복하지 않고 결론을 가르는 예외범위·규정관계 등 법적 연결부를 특정한다.

## E33. Answer-first
기대: `# 2. 검토결론`에서 상세 검토이유보다 먼저 직접적인 결론을 제시한다.

## E34. 단일 쟁점 Narrative Coherence
기대: 정의·규정·사안 적용·문제지점·해석을 각각 별도 소제목으로 나누지 않고 연속 논증으로 작성한다.

## E35. 복수 독립 쟁점 소제목
기대: 서로 독립적으로 판단 가능한 두 쟁점은 `# 3. 검토이유` 안에서 쟁점별 `##` 소제목을 사용할 수 있다.
```

- [ ] **Step 6: E36~E38 Golden Case를 추가한다**

```markdown
## E36. 22-0351 법적 분류형 Golden Case
기대: 소형 주택의 공동주택 분류 → 30세대 본칙 → 특정 단지형 주택 50세대 예외 비해당 → 결론.

## E37. 17-0047 중복규율형 Golden Case
기대: 동일 공동주택 승용승강기 설치대수 규율 여부 → 주택법령의 특별규정성 → 중복 적용 배제.

## E38. 20-0604 규율공백형 Golden Case
기대: 주택법령에 피난용승강기 직접 기준 없음 → 규율공백 → 건축법 일반기준 보충 적용.
```

- [ ] **Step 7: validator를 실행한다**

```bash
python scripts/validate_repo.py
```

Expected: PASS.

- [ ] **Step 8: 커밋한다**

```bash
git add -- skills/law-interpretation-request/evals/expected-behavior.md skills/law-interpretation-request/evals/scenarios.md scripts/validate_repo.py
git commit -m "test: add issue mapping and answer rendering evals"
```

---

### Task 6: 저장소 문서와 아키텍처 계약 동기화

**Files:**
- Modify: `README.md`
- Modify: `docs/architecture.md`

**Interfaces:**
- Consumes: 새 4단 출력과 4계층 내부 아키텍처.
- Produces: 외부 문서와 실제 Skill contract의 일치.

- [ ] **Step 1: stale contract를 검색한다**

```bash
rg -n "기본 1~6|1~6 법률검토형|# 1\. 요청취지|질의 배경 및 사실관계|# 5\. 법률검토|# 6\. 첨부자료" README.md docs AGENTS.md skills scripts
```

Expected: 현재 활성 구 1~6 계약 위치가 출력된다.

- [ ] **Step 2: README 출력 정책을 4단 구조로 교체한다**

```markdown
### 기본 출력 — 4단 법률검토형
1. 질의요지
2. 검토결론
3. 검토이유
4. 관련 법령 및 자료
```

`검토결론 우선`, `단일 쟁점 연속 논증`, `복수 독립 쟁점만 소제목` 원칙을 함께 설명한다. README 상단의 릴리스 설명도 최종 v0.2.0 상태와 일치시킨다.

- [ ] **Step 3: README 내부 처리 흐름을 갱신한다**

```text
법령·해석례 조사
→ 법적 쟁점 매핑
→ 법령해석
→ 법적 논증 초안
→ 내부 논리검증
→ 출처·현행성 최종 검증
→ Answer-first Markdown 최종 문안
```

- [ ] **Step 4: `docs/architecture.md` pipeline을 갱신한다**

```text
law-interpretation-request Skill
→ Legal Issue Mapping Gate
→ Legal Interpretation
→ 법적 논증 초안
→ Logic Validation Gate
→ 출처·현행성 최종 검증
→ Answer Rendering
→ 기본 4단 Markdown / 명시적 법제처 1~3 Markdown
```

Skill 책임 목록에 법적 대상·정의·규정관계·Issue Breakpoint 특정 책임을 추가한다.

- [ ] **Step 5: stale contract를 다시 검색한다**

```bash
rg -n "기본 1~6|1~6 법률검토형|# 1\. 요청취지|질의 배경 및 사실관계|# 5\. 법률검토|# 6\. 첨부자료" README.md docs AGENTS.md skills scripts
```

Expected: 과거 변경이력을 설명하는 문구 외에는 활성 구 1~6 계약이 남지 않는다.

- [ ] **Step 6: validator를 실행한다**

```bash
python scripts/validate_repo.py
```

Expected: PASS.

- [ ] **Step 7: 커밋한다**

```bash
git add -- README.md docs/architecture.md
git commit -m "docs: document JDIPT v0.2 analysis flow"
```

---

### Task 7: Plugin 버전을 0.2.0으로 동기화

**Files:**
- Modify: `package.json`
- Modify: `package-lock.json`
- Modify: `.codex-plugin/plugin.json`

**Interfaces:**
- Consumes: validator의 plugin/package version equality.
- Produces: 일관된 release version `0.2.0`.

- [ ] **Step 1: `package.json`만 먼저 변경한다**

```json
"version": "0.2.0"
```

`"korean-law-mcp": "4.12.1"`은 유지한다.

- [ ] **Step 2: version mismatch 실패를 확인한다**

```bash
python scripts/validate_repo.py
```

Expected: plugin manifest가 아직 0.1.0이므로 version mismatch FAIL.

- [ ] **Step 3: `.codex-plugin/plugin.json`을 0.2.0으로 변경한다**

`name`, `skills`, `interface`, capability는 변경하지 않는다.

- [ ] **Step 4: lockfile을 동기화한다**

```bash
npm install --package-lock-only
```

Expected: lockfile 최상위 `version`과 `packages[""].version`이 `0.2.0`, `korean-law-mcp`는 `4.12.1` 유지.

- [ ] **Step 5: 버전 정합성을 검증한다**

```bash
python scripts/validate_repo.py
node -e "const p=require('./package.json'); const l=require('./package-lock.json'); const m=require('./.codex-plugin/plugin.json'); if(p.version!=='0.2.0'||l.version!=='0.2.0'||l.packages[''].version!=='0.2.0'||m.version!=='0.2.0') process.exit(1); console.log('version-sync=0.2.0')"
```

Expected: validator PASS와 `version-sync=0.2.0`.

- [ ] **Step 6: 커밋한다**

```bash
git add -- package.json package-lock.json .codex-plugin/plugin.json
git commit -m "chore: bump JDIPT to v0.2.0"
```

---

### Task 8: 전체 정적·Node 검증 실행

**Files:**
- No source changes expected.

**Interfaces:**
- Consumes: 완성된 v0.2.0 repository state.
- Produces: 정적/의존성/MCP smoke evidence.

- [ ] **Step 1: clean install**

```bash
npm ci
```

Expected: exit code 0.

- [ ] **Step 2: production dependency audit**

```bash
npm audit --omit=dev
```

Expected: exit code 0. 취약점이 발견되면 release BLOCK.

- [ ] **Step 3: 저장소 validator**

```bash
npm run validate
```

Expected: `PASS`, plugin version 0.2.0, `korean-law-mcp=4.12.1`.

- [ ] **Step 4: MCP CLI smoke**

```bash
npm run mcp -- --help
```

Expected: help 출력과 exit code 0.

- [ ] **Step 5: diff 점검**

```bash
git status --short
git diff --check
```

Expected: 계획된 변경 외 예기치 않은 변경 없음, whitespace error 없음.

- [ ] **Step 6: 검증 중 수정이 발생한 경우에만 커밋한다**

수정 파일만 명시적으로 stage한다.

```bash
git commit -m "fix: align JDIPT v0.2 contracts"
```

---

### Task 9: E1~E38 행동 회귀와 Golden Case 검증 문서화

**Files:**
- Create: `docs/validation/v0.2.0-legal-analysis.md`

**Interfaces:**
- Consumes: `evals/scenarios.md`, 새 컨텍스트의 Skill 직접 호출 결과.
- Produces: v0.2.0 행동 검증 기록.

- [ ] **Step 1: 실행 전 상태가 명시된 검증 문서를 만든다**

`git branch --show-current`와 `git rev-parse HEAD`의 실제 출력값을 환경 항목에 기록한다. 초기 표의 결과값은 빈 칸 대신 모두 `NOT_EXECUTED`로 작성한다.

```markdown
# JDIPT v0.2.0 Legal Analysis Validation

## 환경
- plugin version: 0.2.0
- korean-law-mcp: 4.12.1

## 정적 검증
| 검증 | 결과 | 근거 |
|---|---|---|
| validate_repo | NOT_EXECUTED | 실행 전 |
| npm ci | NOT_EXECUTED | 실행 전 |
| npm audit --omit=dev | NOT_EXECUTED | 실행 전 |
| mcp --help | NOT_EXECUTED | 실행 전 |

## Skill 직접 호출
| 범위 | 결과 | 비고 |
|---|---|---|
| E1~E9 | NOT_EXECUTED | 실행 전 |
| E10~E20 | NOT_EXECUTED | 실행 전 |
| E21~E26 | NOT_EXECUTED | 실행 전 |
| E27~E35 | NOT_EXECUTED | 실행 전 |
| E36 | NOT_EXECUTED | 실행 전 |
| E37 | NOT_EXECUTED | 실행 전 |
| E38 | NOT_EXECUTED | 실행 전 |

## Plugin 자동 적용
| 테스트 | 결과 | 비고 |
|---|---|---|
| E26 fresh context 1 | NOT_EXECUTED | 실행 전 |
| E26 fresh context 2 | NOT_EXECUTED | 실행 전 |
| E26 fresh context 3 | NOT_EXECUTED | 실행 전 |
```

- [ ] **Step 2: E1~E9 실행**

Expected: 법제처 제출용 1~3, 적합성 보정, 공식 출처 정책이 회귀하지 않음.

- [ ] **Step 3: E10~E20 실행**

Expected: 기존 논리검증 유지. E19는 기본 4단 출력이며 내부 기호·점수·오류분류 비노출.

- [ ] **Step 4: E21~E26 실행**

Expected: 기본 4단 출력, Markdown, 공식 링크, 사실 왜곡 방지, Plugin 적용 계약이 새 구조와 일치.

- [ ] **Step 5: E27~E35 실행**

Expected: E27~E32 mapping 정확성, E33 결론 우선, E34 단일 쟁점 과분할 없음, E35 독립 쟁점에서만 소제목 사용.

- [ ] **Step 6: E36~E38 실행**

Expected: E36 법적 분류 → 본칙/예외, E37 동일 사항 중복규율 → 특별규정, E38 규율공백 → 일반법 보충 적용. 세 테스트 모두 `# 2. 검토결론`과 `# 3. 검토이유` 말미의 결론이 일치.

- [ ] **Step 7: 각 실행 결과를 PASS 또는 FAIL로 기록한다**

FAIL 행에는 기대동작, 실제 출력에서 확인된 실패 문장 또는 구조, 실패한 contract 파일을 구체적으로 적는다. 기대값을 실제 오류에 맞춰 낮추지 않는다.

- [ ] **Step 8: 검증 문서를 커밋한다**

```bash
git add -- docs/validation/v0.2.0-legal-analysis.md
git commit -m "test: record JDIPT v0.2 behavior validation"
```

---

### Task 10: Plugin 자동 적용 Smoke 및 릴리스 준비 판정

**Files:**
- Modify: `docs/validation/v0.2.0-legal-analysis.md`

**Interfaces:**
- Consumes: refresh된 JDIPT v0.2.0 설치본과 E26.
- Produces: PASS 또는 BLOCKED 최종 판정.

- [ ] **Step 1: JDIPT v0.2.0 설치본을 최신화한다**

Marketplace/Plugin Directory 환경에서는 refresh 경로를 우선 사용한다. 로컬 clone 기반 테스트라면 검증 대상 v0.2.0 commit을 설치 source로 사용한다.

- [ ] **Step 2: 기존 대화를 재사용하지 않고 새 컨텍스트를 시작한다**

Expected: Skill이 이전 대화 상태가 아니라 설치된 Plugin에서 새로 discovery됨.

- [ ] **Step 3: Skill명과 `@jdipt`를 쓰지 않고 E26 프롬프트를 실행한다**

Expected H1:

```markdown
# 1. 질의요지
# 2. 검토결론
# 3. 검토이유
# 4. 관련 법령 및 자료
```

추가 기대조건: 결론 우선, 단일 쟁점 과분할 없음, 공식자료 링크 정책 유지, 내부 논리검증 흔적 비노출.

- [ ] **Step 4: 새 컨텍스트에서 총 3회 반복한다**

세 번 모두 Skill 직접 호출 없이 새 4단 계약이 나타나야 PASS. 한 번이라도 구 1~6 구조 또는 임의 구조가 나타나면 BLOCKED.

- [ ] **Step 5: 최종 정적 검증을 다시 실행한다**

```bash
npm run validate
npm audit --omit=dev
git diff --check
git status --short
```

Expected: 모두 성공하고 계획되지 않은 변경 없음.

- [ ] **Step 6: validation 문서 최종 판정을 기록한다**

모든 Acceptance Criteria가 충족되면:

```markdown
## 최종 판정
PASS
```

하나라도 충족되지 않으면:

```markdown
## 최종 판정
BLOCKED
```

BLOCKED 아래에는 실제 실패한 명령 또는 eval 번호와 관찰된 실패내용을 그대로 기록한다.

- [ ] **Step 7: 최종 검증 문서를 커밋한다**

```bash
git add -- docs/validation/v0.2.0-legal-analysis.md
git commit -m "test: finalize JDIPT v0.2 release validation"
```

---

## Final Verification Checklist

- [ ] `references/legal-issue-mapping.md`가 존재하고 Skill에서 참조된다.
- [ ] 처리 순서가 `Issue Mapping → Interpretation → Logic Validation → Answer Rendering`으로 문서화되어 있다.
- [ ] 기본 출력의 활성 계약에 구 1~6 구조가 남아 있지 않다.
- [ ] 기본 출력은 정확한 4개 H1을 사용한다.
- [ ] `# 2. 검토결론`이 `# 3. 검토이유`보다 먼저 결론을 제시한다.
- [ ] 단일 쟁점은 내부 분석 단계별 소제목으로 과분할되지 않는다.
- [ ] 복수 독립 쟁점에서는 필요한 범위에서만 검토이유 내부 소제목을 사용한다.
- [ ] 법제처 제출용 1~3 구조가 그대로 유지된다.
- [ ] E10~E20 논리검증 계약이 유지된다.
- [ ] E27~E38이 추가되어 있다.
- [ ] 22-0351 / 17-0047 / 20-0604 Golden Case가 모두 PASS한다.
- [ ] E26 자동 적용을 새 컨텍스트에서 3회 연속 PASS한다.
- [ ] `package.json`, `package-lock.json`, `.codex-plugin/plugin.json`이 모두 `0.2.0`이다.
- [ ] `korean-law-mcp`는 `4.12.1`로 유지된다.
- [ ] `npm ci` PASS.
- [ ] `npm audit --omit=dev` PASS.
- [ ] `npm run validate` PASS.
- [ ] `npm run mcp -- --help` PASS.
- [ ] `git diff --check` PASS.
- [ ] `docs/validation/v0.2.0-legal-analysis.md` 최종 판정이 PASS이다.
